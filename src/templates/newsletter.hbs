<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🐾 {{week}} - Glennallen Panthers Newsletter</title>
    <style>
        /* 🔥 FIERCE PANTHER STYLES 🔥 */
        :root {
            --panther-black: {{theme.primary}};
            --panther-accent: {{theme.accent}};
            --panther-gray: #1a1a1a;
            --panther-silver: #c0c0c0;
            --panther-white: #ffffff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial Black', Arial, sans-serif;
            background: linear-gradient(135deg, var(--panther-black) 0%, var(--panther-gray) 100%);
            color: var(--panther-white);
            line-height: 1.6;
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* 🐾 HEADER STYLES */
        .header {
            text-align: center;
            padding: 40px 20px;
            background: linear-gradient(45deg, var(--panther-black), var(--panther-accent));
            border-radius: 20px;
            margin-bottom: 30px;
            position: relative;
            overflow: hidden;
        }

        .header::before {
            content: '🐾';
            position: absolute;
            top: -20px;
            right: -20px;
            font-size: 150px;
            opacity: 0.1;
            transform: rotate(-15deg);
        }

        .header h1 {
            font-size: 3.5rem;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-bottom: 10px;
            text-shadow: 3px 3px 6px rgba(0,0,0,0.7);
        }

        .header .week-info {
            font-size: 1.3rem;
            color: var(--panther-silver);
            margin-bottom: 20px;
        }

        .mood-indicator {
            display: inline-block;
            font-size: 2rem;
            margin: 10px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        /* 🔥 CONTENT SECTIONS */
        .section {
            background: rgba(255,255,255,0.05);
            border-left: 6px solid var(--panther-accent);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 25px;
            backdrop-filter: blur(10px);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .section:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.3);
        }

        .section-title {
            font-size: 2rem;
            font-weight: 900;
            color: var(--panther-accent);
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 2px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        /* 📚 GRADE SECTIONS */
        .grades-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 25px;
            margin-bottom: 30px;
        }

        .grade-section {
            background: linear-gradient(135deg, rgba(255,255,255,0.1), rgba(255,255,255,0.05));
            border: 2px solid var(--panther-accent);
            border-radius: 20px;
            padding: 25px;
            position: relative;
        }

        .grade-title {
            font-size: 1.8rem;
            font-weight: 900;
            color: var(--panther-accent);
            margin-bottom: 15px;
            text-align: center;
            text-transform: uppercase;
        }

        .content-item {
            margin-bottom: 15px;
            padding: 15px;
            border-radius: 10px;
            transition: all 0.3s ease;
        }

        .content-item:hover {
            transform: translateX(10px);
        }

        .subject {
            font-weight: bold;
            color: var(--panther-accent);
            font-size: 1.1rem;
            margin-bottom: 5px;
        }

        /* 🔥 FIERCENESS LEVELS */
        .panther-mild {
            background: rgba(255,255,255,0.1);
            border-left: 4px solid #888;
        }

        .panther-medium {
            background: rgba(255,215,0,0.1);
            border-left: 4px solid #FFD700;
        }

        .panther-fierce {
            background: rgba(255,69,0,0.2);
            border-left: 4px solid #FF4500;
        }

        .panther-roaring {
            background: rgba(255,0,0,0.2);
            border-left: 4px solid #FF0000;
            animation: fierceGlow 2s infinite alternate;
        }

        @keyframes fierceGlow {
            0% { box-shadow: 0 0 5px rgba(255,0,0,0.3); }
            100% { box-shadow: 0 0 20px rgba(255,0,0,0.7); }
        }

        .roaring {
            color: var(--panther-accent);
            font-weight: 900;
            text-shadow: 1px 1px 3px rgba(0,0,0,0.7);
        }

        .fierce {
            color: #FF4500;
            font-weight: bold;
        }

        /* 🏆 ACHIEVEMENTS */
        .achievements-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
        }

        .achievement {
            background: linear-gradient(45deg, rgba(255,215,0,0.1), rgba(255,140,0,0.1));
            border: 2px solid #FFD700;
            border-radius: 15px;
            padding: 20px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .achievement::before {
            content: '🏆';
            position: absolute;
            top: -10px;
            right: -10px;
            font-size: 60px;
            opacity: 0.2;
        }

        .achievement .student {
            font-size: 1.3rem;
            font-weight: bold;
            color: var(--panther-accent);
            margin-bottom: 10px;
        }

        .achievement .accomplishment {
            font-size: 1.1rem;
            margin-bottom: 10px;
        }

        .achievement .category {
            background: var(--panther-accent);
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9rem;
            display: inline-block;
            text-transform: uppercase;
            font-weight: bold;
        }

        /* 📅 EVENTS */
        .event {
            background: rgba(255,255,255,0.08);
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 15px;
            border-left: 5px solid var(--panther-accent);
        }

        .event-title {
            font-size: 1.4rem;
            font-weight: bold;
            color: var(--panther-accent);
            margin-bottom: 8px;
        }

        .event-details {
            color: var(--panther-silver);
            margin-bottom: 10px;
        }

        .event-description {
            font-size: 1.1rem;
        }

        /* 🌤️ WEATHER SECTION */
        .weather-info {
            background: linear-gradient(45deg, rgba(135,206,235,0.2), rgba(70,130,180,0.2));
            border-radius: 15px;
            padding: 20px;
            text-align: center;
            margin-bottom: 25px;
        }

        /* 📱 RESPONSIVE */
        @media (max-width: 768px) {
            .header h1 { font-size: 2.5rem; }
            .grades-container { grid-template-columns: 1fr; }
            .container { padding: 10px; }
        }

        /* 🎯 PRINT STYLES */
        @media print {
            body { background: white; color: black; }
            .section { break-inside: avoid; }
        }

        /* ✨ FOOTER */
        .footer {
            text-align: center;
            padding: 30px;
            margin-top: 40px;
            border-top: 2px solid var(--panther-accent);
            font-size: 1.1rem;
            color: var(--panther-silver);
        }

        .panther-motto {
            font-size: 1.3rem;
            font-weight: bold;
            color: var(--panther-accent);
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- 🐾 HEADER -->
        <header class="header">
            <h1>🐾 Glennallen Panthers</h1>
            <div class="week-info">{{week}} • {{date}}</div>
            <div class="mood-indicator">{{pantherMood theme.mood}}</div>
            {{#if weather}}
            <div class="weather-info">
                <strong>{{weather.description}}</strong> • {{weather.temperature}}°F
            </div>
            {{/if}}
        </header>

        <!-- 📢 ANNOUNCEMENTS -->
        {{#if announcements}}
        <section class="section">
            <h2 class="section-title">🔥 Announcements</h2>
            {{#each announcements}}
            <div class="content-item panther-fierce">
                {{fierceText this "fierce"}}
            </div>
            {{/each}}
        </section>
        {{/if}}

        <!-- 📚 GRADE SECTIONS -->
        <div class="grades-container">
            <div class="grade-section">
                <h3 class="grade-title">6th Grade Panthers</h3>
                {{#each grades.sixth}}
                <div class="content-item {{fierceClass fierceness}}">
                    <div class="subject">{{subject}}</div>
                    <div class="content">{{fierceText content fierceness}}</div>
                </div>
                {{/each}}
            </div>

            <div class="grade-section">
                <h3 class="grade-title">7th Grade Panthers</h3>
                {{#each grades.seventh}}
                <div class="content-item {{fierceClass fierceness}}">
                    <div class="subject">{{subject}}</div>
                    <div class="content">{{fierceText content fierceness}}</div>
                </div>
                {{/each}}
            </div>

            <div class="grade-section">
                <h3 class="grade-title">8th Grade Panthers</h3>
                {{#each grades.eighth}}
                <div class="content-item {{fierceClass fierceness}}">
                    <div class="subject">{{subject}}</div>
                    <div class="content">{{fierceText content fierceness}}</div>
                </div>
                {{/each}}
            </div>
        </div>

        <!-- 🏆 ACHIEVEMENTS -->
        {{#if achievements}}
        <section class="section">
            <h2 class="section-title">🏆 Panther Achievements</h2>
            <div class="achievements-grid">
                {{#each achievements}}
                <div class="achievement {{fierceClass fierceness}}">
                    <div class="student">{{student}}</div>
                    <div class="accomplishment">{{fierceText accomplishment fierceness}}</div>
                    <div class="category">{{category}}</div>
                </div>
                {{/each}}
            </div>
        </section>
        {{/if}}

        <!-- 📅 UPCOMING EVENTS -->
        {{#if events}}
        <section class="section">
            <h2 class="section-title">📅 Prowling Forward</h2>
            {{#each events}}
            <div class="event">
                <div class="event-title">{{title}}</div>
                <div class="event-details">
                    📅 {{date}}
                    {{#if time}}• ⏰ {{time}}{{/if}}
                    {{#if location}}• 📍 {{location}}{{/if}}
                </div>
                <div class="event-description">{{description}}</div>
            </div>
            {{/each}}
        </section>
        {{/if}}

        <!-- 🐾 FOOTER -->
        <footer class="footer">
            <div>Generated with fierce panther spirit {{#if generatedAt}}on {{generatedAt}}{{/if}}</div>
            <div class="panther-motto">
                {{#if fierceMode}}
                🔥 PROWL WITH PURPOSE • STRIKE WITH PRECISION • ROAR WITH PRIDE 🔥
                {{else}}
                Stay fierce, stay proud, stay Panther! 🐾
                {{/if}}
            </div>
        </footer>
    </div>
</body>
</html>
